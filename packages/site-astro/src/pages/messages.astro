---
const base = process.env.SITE_BASE || import.meta.env.BASE_URL || '/'
const target = `${base.endsWith('/') ? base : `${base}/`}class-space#group-chat`
const redirectContent = `0;url=${target}`
---
<meta http-equiv="refresh" content={redirectContent} />
