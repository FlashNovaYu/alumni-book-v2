# 前端设计系统稳定化验收记录

## 范围

本批次覆盖站点设计令牌入口、后台 UI 基元、名录分页、人物身份共享元素返回、过时测试契约和站点 UI 组件目录收敛。随后补充正式同学 Session 校验，使登录后留言回复使用数据库会话令牌。

## 验证命令

以下命令在分支 `codex/frontend-redesign-stabilization` 上执行，未执行生产部署或生产 D1/R2 写入：

- `pnpm verify:all`
- `pnpm --filter site-astro exec tsx scripts/run-playwright-preview.ts ...`
- `git diff --check`

最终结果：`pnpm verify:all` 通过（Worker 25 个测试文件/201 项，站点静态 26 个文件/175 项，Playwright 61 项全部通过）；`SITE_BASE=/alumni-book-v2` 的后台与站点生产构建通过；`git diff --check` 通过。

完成前若某次 Playwright 运行出现超时，将记录根因修复后的再次运行结果；一次性门禁必须以最终退出码 0 为准。

## 提交记录

- `62c645e`、`23b5041`：恢复共享设计令牌与模块化样式入口
- `e853b37`、`5863eaf`：补齐后台 Dashboard UI 组件和加载无障碍状态
- `b6e0f4e`、`2c7585e`、`4d485ff`：修复名录分页算法、边界和测试契约
- `315ad9b`、`0ab1a3c`：恢复人物长廊共享元素返回目标
- `d3c56f7`：迁移过时站点静态测试契约
- `fcbe079`：收敛无真实消费者的站点 UI 组件并纳入类型检查
- `b50f173`：统一正式同学 Session 与旧留言回复校验
- `98acd96`、`1ca182f`：迁移流程选择器并修复群聊记录抽屉遮挡
- `75aefa6`、`b086b5b`：收紧 CORS/安全头并保护座位、宿舍字段
- `596944c`、`53696fc`：认证失败限流、默认密码失败关闭和 canonical slug
- `3bc7e30`：公开留言/反应限流与访问计数去重
- `07cb468`：相册照片批量查询，消除 N+1
- `4b910ed`：readiness、结构化请求日志、会话清理和 R2 只读巡检
- `f61d312`：后台列表分页、字段裁剪和单页上限
- `4084d4b`：进一步裁剪后台留言与群聊列表字段

## 视觉/交互矩阵

自动化验收覆盖：

- 视口：移动端（约 390×844）、平板（约 768×1024）和桌面（约 1440×900）布局及横向溢出检查；
- 主题：日间纸张主题、夜读主题切换和持久化；
- 动态：`prefers-reduced-motion: reduce` 下的身份转场、主题切换和悬停动画；
- 会话：登录前门控、登录后导航、个人页/信箱/账号中心流程；
- 状态：加载、空状态、错误与重试、群聊同步停止；
- 管理后台：主管理员与权限受限管理员的导航和 Dashboard 入口。

## 已知限制

- Astro 类型检查仍有 10 条既有提示，不影响构建或类型错误门禁；
- 本批次不执行生产部署、生产数据迁移和真实账号认证型生产烟测；
- Session 长期 Cookie 化仍需单独的同源 Cookie/CSRF 迁移设计；当前已完成数据库会话清理、登出撤销和跨请求校验。
- D1 恢复命令与 R2 孤儿对象报告只读流程已记录在 [operations-data-recovery.md](./operations-data-recovery.md)，不会自动修改远程数据。
