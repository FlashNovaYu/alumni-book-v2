# 多管理员后台升级验收记录

验收日期：2026-07-12
验收分支：`codex/admin-rbac-workbench`

## 已验收能力

- 主管理员、内容管理员、内容审核员、运营管理员的服务端实时鉴权；停用、撤销会话和权限调整即时生效。
- 独立账号、绑定同学账号两种登录入口；独立次级账号首次登录或被重置后必须修改密码。
- 权限裁剪工作台、前端路由保护、桌面侧栏与手机底部导航。
- 账号创建、编辑角色、allow/deny 覆盖、重置密码、撤销会话、停用与日志筛选。
- 留言审核、公共留言、相册/照片、时光轴、站点设置、同学档案、通知和上传的管理审计；破坏性操作要求填写原因。
- 同学账号后台入口不在 URL 中传递令牌。

## 自动化验证

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| Worker | `pnpm verify:worker` | 通过，65/65 测试 |
| 后台静态权限检查 | `pnpm --filter admin test:static` | 通过 |
| 后台类型检查与构建 | `pnpm --filter admin build` | 通过 |
| 公开站点类型检查 | `pnpm --filter site-astro typecheck` | 通过 |

## 跨视口检查

本地后台登录页在 `1440×1000`、`768×1024`、`390×844` 下均成功渲染，无横向溢出，浏览器控制台无错误。手机视图的账号密码输入区与登录按钮均保持可触达尺寸。

## 已知外部限制

公开站点的完整 SSG 构建当前未能完成：`packages/site-astro/scripts/fetch-data.ts` 连续访问默认远端接口 `https://alumni-book-api.chenyuhao2263.workers.dev/api/students` 失败，因此没有生成 `packages/site-astro/dist`。依赖该产物的导航、性能和隐私静态测试无法执行；这属于远端数据接口不可达，不是本分支的类型或测试失败。远端接口恢复后应执行：

```powershell
pnpm verify:site
```
