# 多管理员后台升级验收记录

验收日期：2026-07-12
验收分支：`codex/admin-rbac-workbench`

## 已验收能力

- 主管理员、内容管理员、内容审核员、运营管理员的服务端实时鉴权；停用、撤销会话和权限调整即时生效。
- 独立账号、绑定同学账号两种登录入口；独立次级账号首次登录或被重置后必须修改密码。
- 权限裁剪工作台、前端路由保护、桌面侧栏与手机底部导航。
- 账号创建、编辑角色、allow/deny 覆盖、重置密码、撤销会话、停用与日志筛选；重置密码、撤销会话、停用均强制填写原因并写入审计。
- 留言审核、公共留言、相册/照片、时光轴、站点设置、同学档案、通知和上传的管理审计；破坏性操作要求填写原因。
- 同学账号后台入口不在 URL 中传递令牌。
- 上传文件在 R2 写入后，任意数据库读取、准备或原子提交失败都会补偿删除新对象。

## 自动化验证

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| Worker | `pnpm verify:worker` | 通过，8 个测试文件、69 项测试 |
| 后台静态权限检查 | `pnpm --filter admin test:static` | 通过 |
| 后台类型检查与构建 | `pnpm --filter admin build` | 通过 |
| 公开站点完整验证 | 设置本地 Worker 地址后运行 `pnpm verify:site` | 通过，64 项静态测试、22 项浏览器测试 |
| 后台真实流程 | 独立本地 D1、Worker 与 Playwright | 通过主管理初始化、独立审核员首次改密、绑定同学账号首次改密及权限裁剪 |

## 跨视口检查

真实后台流程在 `1440×1000`、`768×1024`、`390×844` 下均成功渲染，无横向溢出，浏览器控制台无错误。主管理员桌面、审核员平板/手机、绑定运营同学手机均验证了实际可见导航与权限边界；手机视图的账号密码输入区与登录按钮保持可触达尺寸。

## 已知外部限制

默认远端接口 `https://alumni-book-api.chenyuhao2263.workers.dev` 在本地验收时不可访问；已改用隔离的本地 Worker 成功完成完整 SSG、导航、性能、隐私和浏览器验证。发布前仍应确认生产 Worker 已部署并可访问。
